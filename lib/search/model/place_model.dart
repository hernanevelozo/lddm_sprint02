class PlacesModel {
  String place_title;
  double place_release_year;
  String place_poster_url;
  double rating;

  PlacesModel(this.place_title, this.place_release_year, this.rating,
    this.place_poster_url);
}
